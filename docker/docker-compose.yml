services:
  pcwcc-build:
    image: pcwcc-release-build:local
    container_name: pcwcc-release-build
    working_dir: /work

    volumes:
      - ${PROJECT_PATH:?Set PROJECT_PATH in .env or shell}:/work

    environment:
      QT_QPA_PLATFORM: offscreen

    tty: true
    stdin_open: true
