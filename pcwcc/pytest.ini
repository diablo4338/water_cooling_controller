[pytest]
asyncio_mode = auto
log_cli = true
log_cli_level = INFO
console_output_style = classic
addopts = -ra -vv
testpaths = tests
markers =
    integration: BLE integration tests that require hardware
    manual: tests that require manual interaction such as pressing a hardware button
    slow: long-running integration tests
