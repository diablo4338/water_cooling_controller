[pytest]
asyncio_mode = auto
log_cli = true
log_cli_level = INFO
console_output_style = classic
addopts = -ra -vv
testpaths = tests
markers =
    integration: BLE integration tests that require hardware
    slow: long-running integration tests
